{
  "name": "@repo/fixtures",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "seed": "bun scripts/seed.ts",
    "reset": "bun scripts/reset.ts",
    "verify": "bun scripts/verify.ts",
    "test": "bun test tests",
    "test:integration": "bun test tests/database.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/schemas": "*"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "typescript": "5.9.2"
  }
}
