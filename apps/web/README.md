# Trace viewer shell

This Next.js/Tailwind application is the read-only UI boundary for the commerce operations investigator.

Phase 2 intentionally provides only a static shell. Trace lookup and rendering remain deferred until Phase 12, after the domain workflow and MCP server are accepted.
